# Example Repo

A minimal example codebase used for testing sema.

## Authentication

Handles JWT generation and validation. See `src/auth/jwt.ts`.

## Session Management

Redis-backed sessions managed via `services/session.py`.

## API Routes

REST endpoints defined in `src/api/routes.ts`.
